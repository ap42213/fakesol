# Frontend Environment Variables

# API URL (backend server) - leave empty in production (same origin)
# In development: http://localhost:3001
VITE_API_URL=http://localhost:3001

# Solana RPC URLs (comma-separated for failover)
# QuickNode recommended: https://quicknode.com (get devnet endpoint)
# Example: VITE_SOLANA_RPC_URLS=https://your-quicknode-endpoint.solana-devnet.quiknode.pro/YOUR_KEY/
VITE_SOLANA_RPC_URLS=
